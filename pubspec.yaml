name: projeto_mobile_faculdade
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  font_awesome_flutter: ^10.8.0
  url_launcher: ^6.3.1
  arcore_flutter_plugin: ^0.1.0
  vector_math: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Adicione a seção assets para incluir a imagem
  assets:
    - lib/assets/notebook.jpg
    - lib/assets/almofada.jpg
    - lib/assets/images.jpeg
    - lib/assets/3d_models/S11.gltf